# Resume here at 4:30 p.m. Edmonton, 12 September 2026

Read `FOLLOWUP_2026-09-12.md`, then `research/09_full_ring_floer_audit.md` and the original `RESEARCH_AUDIT_2026-09-12.md`. **No counterexample is known from this work.** Target D01 = K0 # (-K1); the missing object is an explicit smooth concordance in S3 x I. Non-ribbonness was audited separately.

User authorization: push this repository; keep `main` as the sole branch; Opus critiques are allowed. Do not create branches or switch to the stale saved checkout. Working clone: `/Users/scottg/Documents/Codex/2026-09-12/hey/work/SR-Foxy`. Python: `../knot-venv/bin/python` from that clone.

The earlier pass started at 10% used with five more percentage points allowed, cap 15%; wrapping at 14%. **At 4:30 read a fresh shared usage meter and permit at most 13 additional percentage points, with a buffer.** This is a single authorized follow-up. Pause automation `resume-slice-ribbon-research-at-4-30` after its run; do not schedule another budget. Its prompt already contains this requirement.

Before edits, inspect status and fetch/check origin/main for intervening work. Preserve uncommitted user changes. No need to redo completed checks.

New results: all 60 wider targets pass quotient-chain homotopy retractions for both inputs; full-ring ordinary local-equivalence obstruction is ineffective for the input pair by the exact mixed-lift audit (256 completions, uniform projection). The software/input dependency remains. The second-birth pilot has 7,400 moves, 1,502 distinct diagrams within four runs, 256 sampled HFK checks, three timeouts, 189 passes. No matches to the 1,670-entry K1 index; 12 left and 185 right numerical failures. These are incomplete searches.

Check `results/knotjob/scoped/manifest.json` and `results/knotjob/higher/manifest.json` before planning more Khovanov work. The basic/refined profile completed with zero invariants on K0, K1 and D01; trefoil and ribbon controls passed. Higher Sq2 results may time out; per-case logs preserve partial output. Never treat a timeout or omitted invariant as zero. K2/D02 were deliberately omitted from the bounded scoped runs. The runner has a separate sl3 profile, not yet run.

Final higher-profile status: K1 and D01 both timed out at 120 seconds. Graded-s/BLS finished internally but their numerical values were not printed before timeout. Run each invariant separately to preserve useful output. All session research processes have finished or been stopped before the push.

Local KnotJob JAR: `../knotjob-runtime/KnotJob/KnotJob.jar`. Local Java: `../java-runtime/jdk-25.0.4.1+1-jre/Contents/Home/bin/java`. Official ZIP and portable runtime tarball remain beside the clone; manifest records JAR hash and runtime version. The repository commits inputs and results, not binaries.

Most valuable next actions: finish/check higher Khovanov tests if still unresolved; balance two-birth geometric successors on both sides using saved witnesses and peripheral index; prioritize small non-fibered targets and retain unsampled/failed cases. Intermediate vertices need not pass the final common-target rank test. A complementary HKL refinement requires an explicit identification between the linking-pairing basis and the representation character basis. No claim of isotropy without it.

Any positive match needs independently verified peripheral knot identification, orientation checks, and a replayable annulus movie. A final counterexample needs a smooth disk in standard B4 and a global non-ribbon proof. Neither bounded failure nor an algebraic map supplies either missing geometric object.
