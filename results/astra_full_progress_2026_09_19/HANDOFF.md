# Handoff at the requested pause

**CE: not established. Search: stopped. Watch: disabled. No automatic restart authorized.**

Read `START_HERE.md`, then `PAUSE_STATUS.json` and `RECOVERY_LIMITS.md`. The intact working evidence is `search_run/completed_checkpoint/evidence.sqlite`. Additional partially recovered records are separate under `search_run/recovered_partial/`; do not overwrite the intact database with them.

The captured source tree is at `repository/SR-Foxy/`, pinned to commit 43b71f03a9332af96d96ed1d53370233a1567768. The launched runner lives at `research_jobs/astra_seven_hour_20260919/`. Its deployed copy and controls are also inside the checkpoint. Search run 35455048082 used launch commit 042a7dfc6fe61967902e1c4f7174af5f6342862c, not the later pause/export commit.

The original strategy is preserved in source: common-upper birth/fusion movies for the Abe–Tagami and K7a2/K10n4 pairs, followed by proposed stabilized disk movies for their difference knots with the stevedore and its mirror. Only common-upper segments 1 and 2 finished. Segment 3 was partial. The main stabilized-disk phase was cancelled before execution. A source README describing seven intended segments per phase is not evidence that they ran.

For the two completed segments, the exact counters sum to 167,163 attempted iterations and 21,063 new stored endpoints (plus four roots). Neither completed segment nominated a counterexample. The partial heartbeat adds 34,114 attempts and 4,465 new endpoint events through its timestamp; conservative table recovery preserves a few later records but does not establish a final counter total.

No new mathematical obstruction, concordance, or slice disk is asserted by the archive task. Historical code/results in the repository and previous-session packages retain their original scopes and corrections. Identical storage hashes, valid SQLite, and decompressed move histories do not alone verify oriented surface topology or the nonribbonness hypotheses for a nominated knot.

Before any separately authorized continuation, address checkpoint cancellation consistency. The current runner restarts the segment random seed and adds run rows when called again with the same segment ID; it is not a clean exact mid-segment resumption protocol. Do not double-count earlier time, confuse sampled attempt rows with total counters, silently trust a damaged tail, or rerun a costly job without authorization.

The full package hash verifier is read-only and safe to run while paused:

```sh
python3 VERIFY_ARCHIVE.py
```

No new branch, force-push, replacement of scientific reports, or chat credential is needed merely to inspect this archive. Do not copy the snapshot over an existing newer checkout. The exported Git bundle preserves captured repository ancestry for deliberate offline restoration.
