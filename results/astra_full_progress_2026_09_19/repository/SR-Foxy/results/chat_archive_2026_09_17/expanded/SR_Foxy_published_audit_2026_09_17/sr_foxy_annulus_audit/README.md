# Astra annulus audit: publication and Opus continuation

17 September 2026 UTC / 16 September 2026 in Edmonton.

**NO COUNTEREXAMPLE. No smooth concordance or new slice disk.** Scott authorized publication of Astra's prior audit and requested a prompt for the already-running Opus session. These additions are confined to this new directory on existing `main`; no new branch was created and no Mac checkout was accessed.

## Start here

- `OPUS_PROMPT.md`: continuation prompt. Preserve Opus's live work; do not restart the campaign or duplicate completed searches.
- `REPORT.md`: full proof of the marked-group calculation and local-knotting retraction limitation. Its statements that nothing was pushed describe the earlier audit session, before this publication was authorized.
- `FRAMING_ADDENDUM.md`: a new, explicitly conditional inherited-framing check. The unframed scaffold is NOT globally excluded; the assumption on framings is essential.

The main result remains a construction obstruction, conditional on the upstream diagram-to-marked-group identification. It does not obstruct D01's sliceness. The additional framing calculation only rejects the specified two-handle inherit-and-discard shortcut.

## Reproducible core

`inputs.json`, `verify_annulus_gate.py`, `verification_results.json`, `REPORT.md`, and `coverage.json` are copied unchanged from the prior audit package. `coverage.json` is that prior session's scope record, including its historical empty remote-mutation list. `PUBLICATION_RECHECK.json` records the subsequent reruns and additional reading.

The original archive's 12 listed file hashes were checked before publication. The main checker was run normally and with `-O`; each passed 44 named checks and 4,681 synthetic-word arithmetic controls. Both outputs agreed with the original result in every field except timing. Two deliberately mutated input files were again rejected. These are arithmetic and input-consistency checks, not added knot-search coverage or independent recovery of the geometric inputs.

`verify_framing_gate.py` and `framing_results.json` supply all 16 signed cases of the conditional surgery calculation, plus positive homology controls. The result JSON is compactly serialized; compare parsed JSON when rerunning.

From this directory, use fresh result names:

```
python3 verify_annulus_gate.py --output my_annulus_recheck.json
python3 -O verify_annulus_gate.py --output my_annulus_recheck_optimized.json
python3 verify_framing_gate.py --output my_framing_recheck.json
```

Both scripts use only Python's standard library and refuse to overwrite an existing output file. The complete earlier ZIP and the expanded companion ZIP supplied in the conversation additionally retain duplicate raw replays, mutation inputs and archival logs. This GitHub directory publishes the reusable core and the new handoff, rather than duplicating all those earlier logs.

`PUBLICATION_MANIFEST.json` gives SHA-256 and Git blob SHA-1 values of every other published file in this directory. It excludes itself to avoid a self-hash.

## Unresolved dependency and next construction

The exact algebra uses the recorded group and marked words at input commit `b7ae7270ea6abc6976c0ec598067e2cac3602887`. It does not independently extract them from the original PD, produce actual changed-axis bands, identify the surgery output, embed the required annulus, or prove the new ambient manifold standard.

The next useful object is one actual marked, framed changed-axis/nonlocal construction that escapes the retraction obstruction, followed by an early boundary-identification check and a complete smooth standard-ambient certificate. Algebraically conjugate words and a unimodular surgery matrix are necessary gates in their respective settings, not such a certificate. A substantive construction already produced by Opus takes priority over starting this suggested experiment.

Direct container HTTPS failed at DNS resolution. Publication used the authorized GitHub connector, not the pasted temporary token. No credential was written into these artifacts. Existing repository files and unrelated work were not edited.
