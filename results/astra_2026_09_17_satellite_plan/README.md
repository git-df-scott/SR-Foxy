# Astra's overnight SR-Foxy plan

**No counterexample. No new slice disk.** Created 17 September 2026 UTC, during
the user's evening of 16 September in America/Edmonton.

## Division of work

Opus owns the ongoing changed-axis / genuinely nonlocal concordance
construction. Astra's independent lane is a ribbon-specific satellite
obstruction for the already-slice KDG, with an adversarial audit of any actual
Opus certificate if one arrives. Do not run duplicate D01 geometry searches.

## Completed in this first pass

Derived LOW_WRAPPING_LEMMA.md, with a complete integral-skein proof: under the
recorded KDG cable inputs, the Eisermann nullity and determinant-congruence
tests cannot fire for ANY ribbon pattern of geometric wrapping at most three.
This is not an extrapolation from finitely many passing examples. Two exact
published ribbon-control values force coefficient divisibility; a reference
ribbon knot with determinant 729 transfers the leading residues (25,17,9).
Independent mathematical review is pending, and the original cable geometry
remains an input dependency. Scope is these tests, not all satellites/invariants.

Exact proof-arithmetic checks passed. They are not counts of knots, band
moves, or candidate surfaces. No new satellite was calculated. The first
available higher-support space is {1,z^2,z^4}.

## Ordered research gates for the scheduled passes

1. Attack the new lemma, especially framing/normalization, integral
   coefficients, the annulus proof of exact two-parallel divisibility, and
   the signed Alexander satellite formula. Record a correction if needed.
   Do not spend the night rechecking scalar arithmetic already settled.
2. Build ONE explicit ribbon pattern with genuinely new annular skein
   support at degree four. A marked Bing pattern is a possible first object,
   not yet an accepted diagram or theorem application here. Verify P(U) is
   the unlink by an actual isotopy/movie, calculate its integral annular
   bracket, and reject it cheaply if its support drops to degree <=3 or
   its required coefficient cannot detect an uncomputed term.
3. Derive the precise linear functional of B0(KDG), B2(KDG), B4(KDG) required
   by that pattern. Determine the MINIMAL root-jet order before computing.
   In particular, a two-component degree-four pattern may need only the
   low jet of B4, not the full four-parallel Jones determinant. Do not assume
   either automaticity or independence from the cable test without algebra.
4. Run one bounded target calculation ONLY after the pattern's unknot and
   6_1 controls and a nonslice/violating control pass the identical pipeline.
   Use saved exact inputs, correct zero-framing, signed determinants,
   checkpoints, one worker and explicit time/RAM limits. Missing dependencies
   or timeouts stay UNKNOWN. No large undirected band search.
5. Any violation changes the priority immediately: verify KDG identification,
   its standard-B4 disk dependence on Oliveira-Smith's preprint, the exact
   satellite implication, and independently reproduce the invariant before
   saying counterexample. If no violation, delimit the pattern tested and
   save the next mathematical bottleneck rather than inflate progress.

## Runtime and repository boundaries

This session is a Linux container, not the Mac. Direct network DNS is failing;
SnapPy, Spherogram and Regina are not installed here. The connected GitHub
read/write actions and web research work. Do not claim the Mac is running a
job, promise a continuous process, or schedule a paid runner/API call. If a
later scheduled pass cannot execute a diagram calculation, preserve its
symbolic reduction and executable plan and report the actual limitation.

The user explicitly requested overnight work. A bounded hourly research
schedule was created, beginning approximately 22:00 local and ending in the
morning; these are separate tool-limited passes, not a continuously resident
worker. Each must resume from actual saved state. Preserve prior usage ceilings
when observable; no account meter is accessible here, so percentage usage
cannot be guaranteed. Keep each pass small; no paid APIs or infrastructure.

Use existing main, additive files only, never a new branch, reset, force-push,
or overwrite. Do not touch Opus processes or working files. Read current main
before publishing each timestamped checkpoint under
results/astra_2026_09_17_overnight/. Do not duplicate an overlapping pass.

The morning deliverable is a checkable proof, calculation or exact failed
construction, with a counterexample yes/no statement and one next action.
A progress target is not a promise that a new certificate will be found.

## Files and provenance

LOW_WRAPPING_LEMMA.md: claim, full proof, input dependencies, citations and
review targets. inputs.json: explicitly reused values and source Git blob IDs.
check_transfer.py: deterministic standard-library proof-arithmetic checker.
check_results.json: this pass's completed checks.

Read coverage for this pass: GitHub main state; scripts/jones_root_jet.py;
followup/exact_jet_results.json and root_jet_final/manifest.json; the relevant
primary-source portions listed in the proof. Earlier handed-off notes were
available in the conversation, but the entire repository was not reread.
Large frontier archives were not inspected and no coverage is claimed.
