# A specified product inclusion and the first geometric failure

18 September 2026. Continuation of research/44--47. No counterexample.

**Later update:** research/49 excludes ribbonness of the fixed D01#J using
Agol–Ren's decomposition theorem. The unknown status below records this
earlier checkpoint. Research/49 also identifies A with reversed native c2
and locates the protected-axis meridian that their group equality forgets.

The complete construction and finite artifacts are in
`results/astra_2026_09_18_marked_annulus_construction/`.

| Claim | Evidence | Exact limit |
|---|---|---|
| An explicit product-disk model induces q0 | Whole puncture-sphere collar, upper/lower ball charts, actual pullback of above-plane whisker | Specifies a new explicit completion of the saved marking; endpoint data did not identify a unique historical collar |
| Both original 0110 bands use this marking | Full face routes, opposite neck sides, material ribbon-vector transport, oriented axis traversals | The repaired b' still comes from the research/42 existence surface, without a new PD |
| Two direct compressions fail | Both factor-10 loops have trace 3 in SL(2,F_17), versus identity trace 2; all source relators pass | Excludes those compression loops, not all annuli |
| One fixed ribbon stabilizer J exists | Two parallel product ribbon disks plus one prescribed half-twisted band; explicit C_(2,1)(R) braid and PD | No ribbon certificate for D01#J |
| One ribbon prefix is impossible | The exact cable-splitting saddle leaves a D01#R component, whose K1 summand cannot pair under the existing nonribbon theorem | Does not exclude a different first band or another ribbon disk for the fixed sum |
| The stored D01 has the required unoriented prime summand types | Explicit dual-bigon cut; K0 factor has identical port graph; second factor has a positive 12-tetrahedron identification with mirrored K1, 48 face checks | Upstream triangulation/retriangulation is kernel-trusted; knot orientation is not independently transported |

The sign issue in the collar is substantive: the **inverse** rotation acts on
the target whisker. Tracking the forward rotation would interchange q0 and
q1 while leaving endpoint checks unchanged. Both ball charts and the entire
collar are now written down; the group map is their consequence.

The direct surface attempt reaches genus 14, not genus zero. Its specified
compression loops are not even null-homotopic in the actual chosen disk
exterior. There is therefore no measured annulus primary sum to cancel, no
Whitney-disk intersection ledger, and no changed surgery boundary to which a
nonribbon certificate could be assigned. These missing objects are explicitly
retained as missing.

Commit `ceb83c2` then supplied an alternative one-commutator repair. Its
inputs match the pinned and current checkout. Combining it with our product
surface reduces the available existence construction to genus TWO, with a
potentially different embedded endpoint. Under the actual q0, its A and B
loops have nonidentity matrices `[[16,16],[5,4]]` and `[[1,13],[0,1]]` over
F17. Neither designated cap exists; alternate handles remain unexcluded.
See `ONE_HANDLE_UPDATE.md`. The latter matrix has trace 2, so matrix equality,
not just a trace inequality, matters here.

Following the requested single pivot, J is now fixed. Its obvious cable-band
split cannot begin the required second ribbon disk. The next action is one
mixed splitting band on the saved D01#J diagram, using the shorter A whisker.
Its marked transfer to the cable diagram must be supplied explicitly, followed
by immediate identification of both output components.
